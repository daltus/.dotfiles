TODO prettify

clone into ~
cd into .dotfiles
run ./init_dotfiles.sh (uses bash)

optional:
make ~/.bash_profile_local for machine/company specific config
install vim package manager
install vim solarized colorscheme
